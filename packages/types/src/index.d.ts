/**
 * @grounded/types — Public API
 *
 * Single export surface for all shared Zod schemas and inferred TypeScript types.
 * Both apps/api and apps/web import from here, ensuring the API contract and
 * frontend form validation stay in sync with a single source of truth.
 */
export * from "./schemas/index";
//# sourceMappingURL=index.d.ts.map