export default {
  plugins: {
    tailwindcss: {},
    // Autoprefixer adds vendor prefixes for CSS properties that need them
    // (e.g. -webkit-backdrop-filter for Safari glassmorphism support).
    autoprefixer: {},
  },
};
